Use chezmoi to init the dotfiles on a new machine

## Requirements:
The `loadgemini` function requires the key to be present in the keychain:
`chezmoi secret keyring get --service gemini_api --user=user`
Then enter the API key
